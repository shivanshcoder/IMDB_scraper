from DataBase import iniDB, WORK_CATEGORY

iniDB()


def temptt():
	"""
		@desc:	Scrapes the Data from web and can save it in the object members
		@params:
			(bsObj) BeautifulSoup Object which has the title page
			(loadData) bool object defining whether should it load data in object members
		@return:	None
	"""
	print("Hello")

print (temptt.__doc__)